import { HoverBoxshadowDirective } from './hover-boxshadow.directive';

describe('HoverBoxshadowDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverBoxshadowDirective();
    expect(directive).toBeTruthy();
  });
});
