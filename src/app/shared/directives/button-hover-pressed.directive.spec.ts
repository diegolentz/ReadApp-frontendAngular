import { ButtonHoverPressedDirective } from './button-hover-pressed.directive';

describe('ButtonHoverPressedDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonHoverPressedDirective();
    expect(directive).toBeTruthy();
  });
});
